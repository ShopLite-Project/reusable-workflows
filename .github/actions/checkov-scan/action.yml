name: Checkov IaC Scan
description: Checkout code, run Checkov, and upload the SARIF report

inputs:
  directory:
    description: Directory to scan
    required: false
    default: .
  framework:
    description: Framework to scan (terraform, kubernetes, dockerfile, etc.)
    required: false
    default: all
  soft_fail:
    description: Do not fail on security issues
    required: false
    default: "false"

runs:
  using: composite
  steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Run Checkov
      uses: bridgecrewio/checkov-action@v12
      with:
        directory: ${{ inputs.directory }}
        framework: ${{ inputs.framework }}
        soft_fail: ${{ inputs.soft_fail }}
        output_format: cli,sarif
        output_file_path: console,checkov-results.sarif

    - name: Upload Checkov Report
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: checkov-report
        path: checkov-results.sarif
        retention-days: 30
